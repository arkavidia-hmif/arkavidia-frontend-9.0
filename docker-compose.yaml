version: '3.9'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        API_URI: ${API_URI}
    ports:
      - '${PORT}:${PORT}'
    environment:
      NODE_ENV: ${NODE_ENV}
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
      PORT: ${PORT}
